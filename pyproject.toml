[project]
name = "monads"
version = "0.1.0"
description = "Add your description here"

dependencies = []
readme = "README.md"
requires-python = ">= 3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "ipython>=8.18.1",
]

[tool.hatch.metadata]
allow-direct-references = true
